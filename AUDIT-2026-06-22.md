# 🔍 Audit Report — bahann-pos (LakuPOS)

> **Tanggal:** 22 Juni 2026, 19:47 WIB (updated 20:30 WIB — P0 fixes done)
> **Stack:** Next.js 16 + tRPC + Supabase + Redis
> **Previous audit:** 11 Juni 2026 (`FULL_AUDIT_REPORT.md`)

---

## EXECUTIVE SUMMARY

| Area | Score | Trend | Status |
|------|-------|-------|--------|
| Security | 5/10 | ↓ turun | `Math.random()` di reset token, rate limiter masih in-memory, RLS tanpa policy |
| Architecture | 4/10 | ↓ turun | 8 router bypass use-case layer (naik dari sebelumnya), `any` types meningkat ke 54 |
| Code Quality | 6/10 | ↔ | Error handling membaik (tinggal 4 `throw new Error`), tapi 54 `any` casts |
| Dependencies | 6/10 | ↔ | `pnpm` & `supabase` CLI masih di deps, dual Redis & charting |
| Frontend/UX | 5/10 | ↓ turun | i18n cuma ~10-15%, PaymentModal tanpa a11y, debug UI masih ada |
| Testing | 3/10 | ↔ | 6 test files / 273 source (2.2%), tapi quality test bagus |
| **Overall** | **4.5/10** | ↓ | **Regressi dari 5.5 — banyak issue lama belum fix, muncul issue baru** |

---

## COMPARISON WITH PREVIOUS AUDIT (11 June)

### ✅ Fixed Since Last Audit

| Finding | Status |
|---------|--------|
| File upload validation (magic bytes, MIME, size) | **FIXED** — `normalizeImageUpload()` solid |
| Tenant isolation `getById` (products, outlets) | **FIXED** — filter by `owner_id`/`tenantId` |
| `coverageThresholds` typo in jest.config.js | **FIXED** — sekarang `coverageThreshold` (singular) |
| `ProductRepository` dead code | **NOT DEAD** — ternyata aktif dipakai 8 file |
| Domain service tests (PricingService, StockService, ItemFactory) | **ADDED** — 3 test files, quality excellent |
| CI/CD pipeline | **ADDED** — `.github/workflows/ci.yml` (lint, typecheck, test, build) |
| `Math.random()` in reset token | **FIXED** — `crypto.randomBytes(32)` (22 Jun) |
| `pnpm` in runtime dependencies | **FIXED** — removed from deps (22 Jun) |
| `supabase` CLI in runtime dependencies | **FIXED** — removed from deps (22 Jun) |
| Debug "Lazy Loaded" banner | **FIXED** — removed from LazyDatePicker (22 Jun) |
| `test-payment.html` in public | **FIXED** — deleted (22 Jun) |
| `vercel.json` uses npm | **FIXED** — changed to pnpm (22 Jun) |
| `X-XSS-Protection` deprecated header | **FIXED** — removed, headers consolidated (22 Jun) |

### ❌ Still Broken (from previous audit)

| Finding | Previous | Current |
|---------|----------|---------|
| Rate limiter in-memory `Map` | Flagged | **Still in-memory** — useless di serverless |
| RLS enabled, zero policies | Flagged | **Still no policies** |
| `pnpm` in runtime deps | Flagged | **Still there** |
| `supabase` CLI in runtime deps | Flagged | **Still there** |
| Dual Redis (ioredis + upstash) | Flagged | **Still both** |
| Dual charting (nivo + recharts) | Flagged | **Still both** |
| `throw new Error` in routers | 33 instances | **4 instances** (improved!) |
| DDD violations (routers → infra) | 14/17 routers | **8/22 routers** (worse — new routers added without abstraction) |
| i18n coverage ~30% | Flagged | **~10-15%** (worse — more pages added without i18n) |
| `window.confirm()` / `alert()` | Flagged | **Still 6 occurrences** |
| No root `middleware.ts` | Flagged | **Still missing** |
| `@supabase/auth-helpers-nextjs` deprecated | Flagged | **Still using** — `@supabase/ssr` not installed |

### 🆕 New Findings

| Finding | Severity |
|---------|----------|
| `generateResetToken()` uses `Math.random()` instead of CSPRNG | **CRITICAL** |
| Password reset token lookup accepts plaintext AND hashed | **MAJOR** |
| No Content-Security-Policy header | **MAJOR** |
| `updateUserRole` accepts arbitrary string for role | **MAJOR** |
| `getAllUsers` leaks users across tenants | **MAJOR** |
| `vercel.json` uses `npm` but project uses `pnpm` | **MEDIUM** |
| PaymentModal missing all a11y attributes | **HIGH** |
| `pullProducts()` in sync-manager is a no-op | **HIGH** |

---

## CRITICAL FINDINGS (Fix Immediately)

### C1 — `Math.random()` di password reset token 🔴

**File:** `src/lib/email.ts:649-658`

```typescript
// SEKARANG (INSECURE):
const chars = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
let token = '';
for (let i = 0; i < 64; i++) {
  token += chars.charAt(Math.floor(Math.random() * chars.length));
}

// HARUSNYA:
import { randomBytes } from 'crypto';
const token = randomBytes(32).toString('hex');
```

`Math.random()` bukan CSPRNG — attacker bisa predict token. Codebase sudah punya `crypto.randomBytes` di `refreshToken.ts`, tinggal pakai pattern yang sama.

### C2 — Rate limiter masih in-memory `Map` 🔴

**File:** `src/lib/security/rateLimiter.ts:11`

Masih `new Map()`. Di Vercel/serverless, setiap cold start reset. Attacker bisa bypass dengan hit instance berbeda. Login (5/15min) dan password reset (3/15min) sudah di-apply tapi **tidak efektif**.

**Fix:** Pakai `@upstash/redis` yang sudah ter-install.

### C3 — RLS enabled, zero policies 🔴

**File:** `supabase/migrations/016_enable_rls.sql`

RLS on di 14 tables, tapi **zero policies**. App pakai `supabaseAdmin` (service role) yang bypass RLS. Kalau service role key leak = **full database access**.

---

## MAJOR FINDINGS

### M1 — 8 router bypass DDD layer entirely

| Router | `supabaseAdmin` calls | Use-case? |
|--------|----------------------|-----------|
| `posDevices.ts` | 12 | ❌ None |
| `transactionApprovals.ts` | 13 | ❌ None |
| `expenses.ts` | 4 | ❌ None |
| `outletGroups.ts` | 1+inline | ❌ None |
| `shifts.ts` | 2 RPC | ❌ None |
| `transactions.ts` | 2 RPC | Partial |
| `payments.ts` | 2 | Partial |
| `users.ts` | 1 | Partial |

4 router baru (posDevices, transactionApprovals, expenses, outletGroups) ditambahkan tanpa abstraction layer sama sekali — semua logic inline di tRPC handler.

### M2 — Password reset token accept plaintext fallback

**File:** `src/infra/repositories/SupabasePasswordResetTokenRepository.ts:11`

```typescript
.in('token', [token, hashedToken]) // accepts BOTH
```

New tokens hashed, tapi query masih accept plaintext. Old plaintext tokens di DB masih valid. Plaintext fallback harus dihapus.

### M3 — No Content-Security-Policy header

`next.config.ts` set security headers tapi **tanpa CSP**. Untuk POS app yang handle transaksi finansial, CSP adalah layer pertahanan penting melawan XSS.

### M4 — `getAllUsers` leaks cross-tenant data

**File:** `src/server/routers/auth.ts:284-308`

`adminProcedure` + `findAll()` tanpa filter `tenant_id`. Admin tenant A bisa lihat users dari tenant B.

### M5 — `updateUserRole` no role enum validation

**File:** `src/server/routers/superAdmin.ts:180`

```typescript
role: z.string() // accepts ANY string
```

Harusnya `z.enum(['admin', 'cashier', 'manager', ...])`.

### M6 — No root `middleware.ts`

Edge-level route protection tidak ada. Semua auth rely di tRPC procedure guards. Tambah Next.js API route atau page tanpa guard = unprotected.

### M7 — `@supabase/auth-helpers-nextjs` deprecated

Masih dipakai di 3 file. `@supabase/ssr` belum di-install.

### M8 — PaymentModal zero a11y

**File:** `src/components/payment/PaymentModal.tsx`

Tanpa `role="dialog"`, `aria-modal`, focus trap, ESC handler. Ini modal pembayaran — salah satu UI paling kritis.

---

## SECURITY AUDIT DETAIL

### Authentication & Authorization

| # | Severity | Finding | File | Line |
|---|----------|---------|------|------|
| S1 | ✅ FIXED | `generateResetToken()` uses `Math.random()` → now `crypto.randomBytes` | `src/lib/email.ts` | 649-658 |
| S2 | ✅ FIXED | Token lookup accepts plaintext + hashed → now hashed only | `SupabasePasswordResetTokenRepository.ts` | 11 |
| S3 | **MAJOR** | No root `middleware.ts` for route protection | (missing) | — |
| S4 | **MINOR** | JWT fallback default 7 days | `src/lib/jwt.ts` | 13 |
| S5 | **MINOR** | `decodeJWT` exported but unused | `src/lib/jwt.ts` | 49-56 |
| S6 | **MINOR** | `sameSite: 'lax'` instead of `'strict'` | `src/lib/cookies.ts` | 22-24 |

### Rate Limiting

| # | Severity | Finding | File | Line |
|---|----------|---------|------|------|
| S7 | ✅ FIXED | In-memory `Map` → Redis-backed via Upstash | `src/lib/security/rateLimiter.ts` | 11 |
| S8 | **MINOR** | `resetRateLimit` exported, unused | `src/lib/security/rateLimiter.ts` | 96 |

### Input Validation

| # | Severity | Finding | File | Line |
|---|----------|---------|------|------|
| S9 | **MAJOR** | `role` input no enum validation | `src/server/routers/superAdmin.ts` | 180 |
| S10 | **MINOR** | Search input PostgREST wildcard injection | `superAdmin.ts` / `SupabaseUserRepository.ts` | 144, 366 |
| S11 | ✅ | File upload validation robust | `src/lib/security/imageUpload.ts` | — |

### Tenant Isolation

| # | Severity | Finding | File | Line |
|---|----------|---------|------|------|
| S12 | **MAJOR** | `getAllUsers` cross-tenant leak | `src/server/routers/auth.ts` | 284-308 |
| S13 | ✅ | `getById` products/outlets scoped | `products.ts` / `outlets.ts` | — |
| S14 | ✅ | `superAdminProcedure` reads role from DB | `src/server/trpc.ts` | 166-184 |

### Database & RLS

| # | Severity | Finding | File | Line |
|---|----------|---------|------|------|
| S15 | ✅ FIXED | RLS enabled, zero policies → migration 041 adds tenant-scoped policies | `supabase/migrations/041_rls_policies.sql` | — |
| S16 | **MAJOR** | `supabaseAdmin` used everywhere, bypasses RLS | Multiple routers | — |
| S17 | **MINOR** | Dual env var fallback for service key | `src/infra/supabase/server.ts` | 4 |

### CSRF, XSS, Headers

| # | Severity | Finding | File | Line |
|---|----------|---------|------|------|
| S18 | **MAJOR** | No Content-Security-Policy header | `next.config.ts` | — |
| S19 | ✅ | No `dangerouslySetInnerHTML` / `eval()` | — | — |
| S20 | ✅ | Security headers configured (HSTS, X-Frame, etc.) | `next.config.ts` | 39-75 |
| S21 | **MINOR** | No CORS config (acceptable for same-origin) | — | — |

**Totals: 1 CRITICAL, 8 MAJOR, 7 MINOR, 5 positive findings**

---

## ARCHITECTURE & CODE QUALITY DETAIL

### DDD Layer Violations

**8 routers bypass use-case layer** (import `supabaseAdmin` directly):
- `posDevices.ts` (12 calls, no use-case)
- `transactionApprovals.ts` (13 calls, no use-case)
- `expenses.ts` (4 calls, no use-case)
- `outletGroups.ts` (1+inline, no use-case)
- `shifts.ts` (2 RPC, no use-case)
- `transactions.ts` (2 RPC, partial)
- `payments.ts` (2, partial)
- `users.ts` (1, partial)

**3 routers instantiate repos directly** (bypass DI container):
- `businessProfile.ts:10` — `new SupabaseBusinessProfileRepository()`
- `sales.ts:8` — `new SupabaseDailySaleRepository()`
- `outlets.ts:56` — `new SupabaseUserManagementRepository()`

**1 router imports use-cases directly** instead of container:
- `auth.ts:4-6` — imports LoginUserUseCase, RegisterUserUseCase, LogoutUserUseCase

### Container Stats
- 18 domain repository interfaces
- 19 infra repository implementations
- 19 repos registered in container
- 22 routers total

### Error Handling

| Finding | Count | Location |
|---------|-------|----------|
| `throw new Error` in routers (should be TRPCError) | 4 | `superAdmin.ts:152,193,198,207` |
| `throw new TRPCError` (correct) | ~193 | All routers |
| Competing error hierarchies | 3 | `AppError` class, `AppError` interface, `DomainException` |
| String-matching for error codes | 1 | `cashSessions.ts:72-74` |

### TypeScript Quality

| Finding | Count |
|---------|-------|
| `any` type usage | **54 instances** |
| `as unknown as` double-casts | **12 instances** |
| Worst offender | `products/page.tsx` (11 `any` casts) |

Top files with `any`:
| File | Count |
|------|-------|
| `src/app/(app)/products/page.tsx` | 11 |
| `src/app/(app)/approvals/page.tsx` | 6 |
| `src/app/(app)/settings/subscriptions/page.tsx` | 4 |
| `src/server/routers/users.ts` | 3 |
| `src/app/(app)/warehouse/movements/page.tsx` | 3 |
| `src/app/(app)/settings/users/page.tsx` | 3 |

### Code Duplication

| Pattern | Instances | Location |
|---------|-----------|----------|
| `getTenantOutletIds` | 2 definitions | `server/lib/tenant.ts:73`, `SupabaseAdminRepository.ts:5` |
| `tenant.ts` mixing concerns | 1 file, 13 importers | Infra + auth + tRPC in one file |

### Dead Code

| Item | Status |
|------|--------|
| `src/domain/services/BusinessTypeStrategy.ts` | Does not exist, never referenced |
| `src/types/index.ts:76` `interface AppError` | Dead — never imported |
| `DOMAIN_ERROR_MESSAGES_ID` / `DOMAIN_ERROR_MESSAGES_EN` | Defined but unused in production |
| `decodeJWT` | Exported but never imported |

---

## DEPENDENCIES DETAIL

### HIGH — Must Fix

| # | Issue | File | Fix |
|---|-------|------|-----|
| D1 | `pnpm` in runtime `dependencies` | `package.json:45` | Remove — use `corepack` or `packageManager` field |
| D2 | `supabase` CLI in runtime `dependencies` | `package.json:52` | Remove — `npx supabase` in script is enough |
| D3 | `@supabase/auth-helpers-nextjs` deprecated | `package.json:27` | Migrate to `@supabase/ssr` |

### MEDIUM

| # | Issue | Fix |
|---|-------|-----|
| D4 | Dual Redis: `ioredis` + `@upstash/redis` | Remove `ioredis`, use upstash for health checks too |
| D5 | Dual charting: `@nivo/*` + `recharts` | Migrate 1 nivo component to recharts, remove nivo |
| D6 | `vercel.json` uses `npm` but project uses `pnpm` | Change to `pnpm install`/`pnpm build` |

### LOW

| # | Issue | Status |
|---|-------|--------|
| D7 | `@solana/web3.js` (~2-5MB) | Used for crypto payments — consider dynamic import |
| D8 | `puppeteer` in devDeps | Justified — used in `generate-pdf.js` |
| D9 | `pg` + `@types/pg` in devDeps | Justified — used in admin scripts |
| D10 | `marked` in devDeps | Justified — used in `generate-pdf.js` |
| D11 | `xlsx` | Properly dynamically imported, Apache-2.0 license |
| D12 | Security headers duplicated (next.config.ts + vercel.json) | Consolidate to `next.config.ts` |

### Lock File Issue

Both `pnpm-lock.yaml` AND `package-lock.json` exist. `vercel.json` uses `npm install` which uses `package-lock.json`. Local dev uses pnpm. This can cause dependency resolution differences between environments.

---

## FRONTEND & UX DETAIL

### Debug UI (HIGH)

| # | Finding | File | Line |
|---|---------|------|------|
| F1 | "Lazy Loaded" banner visible in production | `LazyDatePicker.tsx` | 155-160 |
| F2 | `test-payment.html` left in `public/` | `public/test-payment.html` | — |

### alert() / confirm() (HIGH)

| # | Type | File | Line | Message |
|---|------|------|------|---------|
| F3 | `alert()` | `PrintPreviewModal.tsx` | 37 | 'Gagal menyiapkan print...' |
| F4 | `alert()` | `pos/sales/page.tsx` | 116 | 'Gagal menyiapkan print...' |
| F5 | `alert()` | `settings/subscriptions/page.tsx` | 232 | 'Ukuran file maksimal 5MB' |
| F6 | `alert()` | `admin/settings/page.tsx` | 190 | 'Maksimal 5MB' |
| F7 | `confirm()` | `outlets/page.tsx` | 92 | 'Hapus outlet "${name}"?' |
| F8 | `confirm()` | `products/page.tsx` | 346 | 'Hapus "${name}"?' |

### Accessibility (CRITICAL/HIGH)

| # | Severity | Finding | File |
|---|----------|---------|------|
| F9 | **CRITICAL** | PaymentModal: no `role="dialog"`, no aria, no focus trap, no ESC | `PaymentModal.tsx` |
| F10 | **HIGH** | BulkImportModal bypasses shared Modal, no a11y | `BulkImportModal.tsx` |
| F11 | **HIGH** | PrintPreviewModal bypasses shared Modal, no a11y | `PrintPreviewModal.tsx` |
| F12 | **HIGH** | Shared Modal missing focus trap (WCAG 2.4.3) | `Modal.tsx` |
| F13 | **MEDIUM** | Client-side auth guard via `localStorage.getItem('user')` | `(app)/layout.tsx:26` |
| F14 | **MEDIUM** | Dead code: `localStorage.removeItem('auth_token')` in Sidebar | `Sidebar.tsx:182` |
| F15 | **LOW** | PaymentModal `setTimeout` risk after unmount | `PaymentModal.tsx:142-148` |

### i18n (CRITICAL)

- Locale files: 432 keys each (en.json, id.json) — perfectly matched
- **Only 5 pages** use `useLanguage()`: login, dashboard, about, setup, register
- **28 pages** do NOT use i18n: POS, products, transactions, payments, expenses, EOD, all warehouse, all admin, all settings, promotions, approvals, outlets
- **Only 1 component** (Sidebar) uses `t()`
- Coverage: **~10-15%** of UI

### PWA / Offline (HIGH)

| # | Finding | File |
|---|---------|------|
| F16 | `pullProducts()` entirely commented out (no-op) | `sync-manager.ts:267-313` |
| F17 | No Background Sync API | — |
| F18 | Dual service worker registration | `PWAInstaller.tsx` + `PWAContext.tsx` |
| F19 | Offline data uses localStorage, not IndexedDB | `pos/sales/page.tsx` |

---

## TESTING DETAIL

### File Counts

| Metric | Value |
|--------|-------|
| Source files (`.ts`/`.tsx`) | **273** |
| Test files (`.test.*`/`.spec.*`) | **6** |
| Test-to-source ratio | **2.2%** |

### Test Inventory

| File | Type | Quality |
|------|------|---------|
| `Button.test.tsx` | UI component | Good — behavior, userEvent, CSS classes |
| `Card.test.tsx` | UI component | Good — structural elements |
| `PricingService.test.ts` | Domain service | Excellent — happy/error/edge cases |
| `StockService.test.ts` | Domain service | Excellent — null stock, exact-to-zero |
| `ItemFactory.test.ts` | Domain service | Excellent — validation combos |
| `e2e/login.spec.ts` | E2E | Good — 5 tests, 3 browsers |

### Untested Layers

| Layer | Files | Status |
|-------|-------|--------|
| tRPC routers | ~22 | **UNTESTED** |
| Use-cases | ~25 | **UNTESTED** |
| Infra repositories | ~19 | **UNTESTED** |
| Lib (auth, payment, utils) | ~30 | **UNTESTED** |
| Components (except Button, Card) | ~20 | **UNTESTED** |
| Pages/layouts | ~48 | **UNTESTED** |

### CI/CD

Pipeline exists at `.github/workflows/ci.yml`:
- Lint → Typecheck → Test → Build (gated)
- Node 20, pnpm, `cancel-in-progress: true`
- No E2E in CI

### Jest Config

- `coverageThreshold` set at 50% globally — almost certainly failing in CI
- No `noUnusedLocals`/`noUnusedParameters` in tsconfig

---

## QUICK WINS (< 5 menit each)

| # | Task | File | Impact | Status |
|---|------|------|--------|--------|
| 1 | `Math.random()` → `crypto.randomBytes()` | `src/lib/email.ts:649` | Fix CRITICAL security | ✅ DONE |
| 2 | Remove `pnpm` from dependencies | `package.json` | Clean deps | ✅ DONE |
| 3 | Remove `supabase` CLI from dependencies | `package.json` | Clean deps | ✅ DONE |
| 4 | Remove debug "Lazy Loaded" banner | `LazyDatePicker.tsx:155` | No debug UI | ✅ DONE |
| 5 | Delete `public/test-payment.html` | `public/` | No test file in prod | ✅ DONE |
| 6 | `vercel.json` → `pnpm install`/`pnpm build` | `vercel.json` | Consistent PM | ✅ DONE |
| 7 | Remove `X-XSS-Protection` (deprecated) + consolidate headers | `next.config.ts` + `vercel.json` | Modern headers | ✅ DONE |

---

## PRIORITIZED ACTION PLAN

### P0 — CRITICAL (Before next deploy)

| # | Task | Effort | Impact | Status |
|---|------|--------|--------|--------|
| 1 | Fix `generateResetToken` — use `crypto.randomBytes` | 5m | Prevents token prediction | ✅ DONE |
| 2 | Replace in-memory rate limiter with Redis-backed | 2h | Actual rate limiting | ✅ DONE |
| 3 | Add RLS policies for sensitive tables | 3h | Defense-in-depth | ✅ DONE |
| 4 | Remove plaintext token fallback in `findByToken` | 15m | Clean auth path | ✅ DONE |
| 5 | Fix `vercel.json` to use pnpm | 5m | Consistent deploys | ✅ DONE |

### P1 — HIGH (This week)

| # | Task | Effort | Impact |
|---|------|--------|--------|
| 6 | Add Content-Security-Policy header | 1h | XSS mitigation |
| 7 | Scope `getAllUsers` by tenant_id | 30m | Fix cross-tenant leak |
| 8 | Validate role input with enum | 15m | Input validation |
| 9 | Create root `middleware.ts` | 2h | Edge auth |
| 10 | Fix PaymentModal a11y | 2h | Critical modal accessibility |
| 11 | Remove `pnpm`, `supabase` CLI from deps | 5m | Clean bundle |
| 12 | Remove debug "Lazy Loaded" UI | 5m | No debug in prod |

### P2 — MEDIUM (This sprint)

| # | Task | Effort | Impact |
|---|------|--------|--------|
| 13 | `throw new Error` → `TRPCError` (4 in superAdmin) | 30m | Proper error codes |
| 14 | Replace `any` types (54 instances) | 4h | Type safety |
| 15 | Consolidate dual Redis → upstash only | 1h | Simpler infra |
| 16 | Consolidate dual charting → recharts only | 1h | Smaller bundle |
| 17 | Migrate `@supabase/auth-helpers-nextjs` → `@supabase/ssr` | 2h | Future-proof auth |
| 18 | Add use-case tests (highest ROI) | 4h | Business logic coverage |
| 19 | Replace `alert()`/`confirm()` with Toast/Modal (6 places) | 1h | Consistent UX |
| 20 | Implement `pullProducts()` in sync-manager | 2h | Functional offline |

### P3 — LOW (Backlog)

| # | Task | Effort | Impact |
|---|------|--------|--------|
| 21 | Complete i18n coverage (~85% remaining) | 12h+ | Multi-language |
| 22 | Add Background Sync API to service worker | 2h | Reliable offline |
| 23 | Extract `tenant.ts` into proper service | 2h | Clean architecture |
| 24 | Align Supabase types with domain entities | 3h | Remove double-casts |
| 25 | Add E2E tests for POS flow, transactions | 4h | Critical path coverage |

---

## FINDINGS SUMMARY BY SEVERITY

### CRITICAL (3)
- C1: `Math.random()` in reset token generation
- C2: Rate limiter in-memory (serverless useless)
- C3: RLS enabled, zero policies

### MAJOR (12)
- M1: 8 routers bypass DDD layer
- M2: Password reset accepts plaintext fallback
- M3: No Content-Security-Policy header
- M4: `getAllUsers` cross-tenant leak
- M5: `updateUserRole` no role enum
- M6: No root `middleware.ts`
- M7: `@supabase/auth-helpers-nextjs` deprecated
- M8: PaymentModal zero a11y
- S7: Rate limiter in-memory (same as C2)
- S9: Role input no enum (same as M5)
- S12: Cross-tenant leak (same as M4)
- S15/S16: RLS issues (same as C3)

### MINOR (12)
- S4: JWT fallback 7d default
- S5: `decodeJWT` unused
- S6: `sameSite: 'lax'`
- S8: `resetRateLimit` unused
- S10: PostgREST wildcard injection
- S17: Dual env var fallback
- S21: No CORS config
- D4-D6: Dependency issues
- F13-F15: Frontend minor issues
- Dead code: 3 items
- tsconfig: no unused checks

### FIXED (6)
- File upload validation
- Tenant isolation getById
- coverageThreshold typo
- ProductRepository not dead
- No dangerouslySetInnerHTML
- Security headers configured

---

*Audit compiled from 5 parallel agents: Security, Architecture, Dependencies, Testing, Frontend/UX*
*Next review scheduled: nanti malam (22 Juni 2026)*
